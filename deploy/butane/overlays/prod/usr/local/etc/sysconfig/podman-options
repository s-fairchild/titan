# shellcheck shell=bash
# shellcheck disable=SC2034

# CLUSTER=rick
# Applied as a label for podman components
CLUSTER=rick

# DNS_SEARCH="expresso.lan"
# Assigned to container instance DNSSearch as a systemd environment variable
DNS_SEARCH=expresso.lan
