---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

secretGenerator:
- name: traefik-values
  namespace: kube-system
  files:
  - values=values.yaml
  options:
    disableNameSuffixHash: true
