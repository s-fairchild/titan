---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: default
  namespace: jellyfin
spec:
  secretName: default
  dnsNames:
    - "jellyfin.app.rick.expresso.lan"
  issuerRef:
    name: my-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
