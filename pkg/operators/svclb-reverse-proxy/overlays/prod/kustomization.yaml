apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

configMapGenerator:
  - name: reverse-proxy-config
    namespace: traefik-system
    files:
      - assets/nginx.conf
